import RegistrationForm from "~/components/Registration/RegistrationForm";

const registration = () => {
    return (
        <div>
            <RegistrationForm/>
        </div>
    );
};

export default registration;